import 'package:flutter/material.dart';

class AppFlavors extends StatelessWidget {
  const AppFlavors({Key? key, required this.flavor}) : super(key: key);
  final String flavor;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
